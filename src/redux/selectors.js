export const getFilterSelector = state => state.filter;
export const getContactsSelector = state => state.contacts.items;
export const getIsLoadingSelector = state => state.contacts.isLoading;
export const getErrorSelector = state => state.contacts.error;
